# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Data / ORM
sqlalchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3       # PostgreSQL driver (production); SQLite needs none

# Validation / settings
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# Security
argon2-cffi==23.1.0          # Argon2id password hashing (OWASP recommended)
pyjwt==2.10.1                # JWT access / refresh tokens

# Misc
python-multipart==0.0.20     # form parsing (login)

# AI assistant
httpx==0.28.1                # calling LLM provider HTTP APIs
pypdf==5.1.0                 # extracting lesson PDF text for grounding

# Off-box backup storage (S3-compatible: R2, B2, S3, MinIO)
boto3==1.35.92

# Reports
python-docx==1.1.2           # generating Word (.docx) reports

# Slide rendering for the vision-capable teacher assistant
pymupdf==1.25.1              # renders one PDF page to an image (no system deps)

# Tests
pytest==8.3.4
moto[s3]==5.0.28          # a real S3 implementation, so backup tests are not just fakes
